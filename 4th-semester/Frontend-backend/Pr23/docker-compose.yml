version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend1
      - backend2
    networks:
      - app-network

  backend1:
    build: .
    environment:
      - SERVER_ID=backend-1
    networks:
      - app-network

  backend2:
    build: .
    environment:
      - SERVER_ID=backend-2
    networks:
      - app-network

networks:
  app-network:
    driver: bridge